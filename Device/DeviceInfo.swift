//
//  DeviceInfo.swift
//  metamask-ios-sdk
//

import UIKit
import Foundation

public struct DeviceInfo {
    public static let platformDescription = "\(UIDevice.current.name) \(UIDevice.current.systemName) \(UIDevice.current.systemVersion)"
}
