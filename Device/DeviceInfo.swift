//
//  DeviceInfo.swift
//  metamask-ios-sdk
//

import Foundation

struct DeviceInfo {
    static let platformDescription = "\(UIDevice.current.name) \(UIDevice.current.systemName) \(UIDevice.current.systemVersion)"
}
